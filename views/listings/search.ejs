<h1>Search Results for "<%= query %>"</h1>

<% if (results.length > 0) { %>
    <ul>
        <% results.forEach(result => { %>
            <li><a href="/listings/<%= result._id %>"><%= result.title %></a></li>
        <% }) %>
    </ul>
<% } else { %>
    <p>No results found.</p>
<% } %>

